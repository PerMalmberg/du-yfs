local destBehaviour = {}
destBehaviour.__index = destBehaviour

local function new(controller)
    local instance = {}

    setmetatable(instance, destBehaviour)

    return instance
end

-- The module
return setmetatable(
    {
        new = new
    },
    {
        __call = function(_, ...)
            return new(...)
        end
    }
)
